name: exp003-adversarial-audit

on:
  push:
    branches:
      - exp003-adversarial-audit
    paths:
      - "src/lifelike_min/exp003_audit.py"
      - ".github/workflows/exp003-audit.yml"
  pull_request:
    paths:
      - "src/lifelike_min/exp003_audit.py"
      - ".github/workflows/exp003-audit.yml"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - name: Check out branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run adversarial audit
        env:
          PYTHONPATH: src
        run: |
          mkdir -p results/exp003-audit
          python -m lifelike_min.exp003_audit --json results/exp003-audit/audit.json

      - name: Preserve audit artifact
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: exp003-adversarial-audit
          path: results/exp003-audit/audit.json
          if-no-files-found: error
